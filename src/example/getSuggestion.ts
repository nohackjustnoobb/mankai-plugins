async function getSuggestion(query: string): Promise<string[]> {
  // TODO: Implement the logic to retrieve manga suggestions based on the query
  console.log(`Retrieving suggestions for query: ${query}`);
}

export default getSuggestion;
